# ─── AlphaEdge environment ───────────────────────────────
# Copy to .env.local and fill in. ALL ARE OPTIONAL —
# the app runs on deterministic mock data when these are unset.

# Live market data (quotes, news, symbol search) — free tier at finnhub.io
FINNHUB_API_KEY=

# Real AI document analysis on the /documents page — platform.claude.com
ANTHROPIC_API_KEY=
